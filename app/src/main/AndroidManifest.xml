<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vimukthisadaruwan65gmail.testing">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logooo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Main2Activity"
            android:parentActivityName=".MainActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity android:name=".fbwebpage" />
        <activity android:name=".twitterwebpage" />
        <activity android:name=".linkedinwebpage" />
        <activity android:name=".insterwebactivity" />
        <activity android:name=".gwebpage" />
        <activity android:name=".wordpresswebpage" />
        <activity android:name=".blogeerwebpage" />
        <activity android:name=".youtubewebpage" />
        <activity android:name=".dropboxwebpage" />
        <activity android:name=".Main3Activity"></activity>
    </application>

</manifest>